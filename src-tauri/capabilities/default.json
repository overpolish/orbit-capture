{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for Orbit Capture windows",
  "windows": ["recording-bar", "permissions"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:deny-internal-toggle-maximize"
  ]
}
